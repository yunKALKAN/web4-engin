version: "3.9"

services:
  api-gateway:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5002:5002"
    env_file:
      - ../.env
    volumes:
      - ..:/app
    command: uvicorn apps.api.main:app --host 0.0.0.0 --port 5002 --reload
    restart: unless-stopped
