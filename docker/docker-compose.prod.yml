version: "3.9"

services:
  api-gateway:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    ports:
      - "5002:5002"
    env_file:
      - ../.env
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
