# Legacy

This directory contains legacy Flask-based API code.

The official API framework is **FastAPI** (see `apps/api/main.py`).

These files are kept for reference only and are not actively maintained.
